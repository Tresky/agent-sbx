#!/usr/bin/env python3
import sys
from pathlib import Path

if sys.version_info < (3, 11):
    sys.exit("sbx needs Python 3.11 or newer (for tomllib)")

sys.path.insert(0, str(Path(__file__).resolve().parent.parent))
from sbxlib.cli import main  # noqa: E402

sys.exit(main())
