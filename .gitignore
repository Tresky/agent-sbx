__pycache__/
*.pyc
.DS_Store
# A real key must never land in an example.
examples/**/config/master.key
examples/**/.env*
# Each setup's own values. host/local.conf.example shows what goes in it.
host/local.conf
# Each person's own template definitions and components.
templates/local/
template/components/local/
