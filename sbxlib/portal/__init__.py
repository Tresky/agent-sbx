"""The sbx portal: a local web page for everything sbx does.

`sbx web` starts it (server.py). The routes are in api.py, the long actions
run as jobs (jobs.py), and the page is static/ with no build step and no
dependency, like the rest of sbx.
"""
